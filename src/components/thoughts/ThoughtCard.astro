---
const { article, tags, link } = Astro.props

console.log(link)

---
 
<article class="">
        <div class="max-w-sm border mx-auto shadow bg-gray-950 dark:border-gray-700 mb-20 h-[30rem]">
            <a href={`/${link}`}>
                <img class="hover:grayscale transition ease-in-out duration-300" src={`${article.image.filename}`} alt="" />
                <div class="p-5 pt-0">
                    <p class="mt-4 -mb-3 text-sm">📅 {article.date}</p>
                    <h2 class="mb-2 text-xl tracking-tight">{article.title}</h2>
                    <p class="mb-3 font-normal text-white mt-3">{article.description}</p>
                </div>
            </a>
        </div>
</article>